version: '3.8'

services:
  api:
    build: .
    container_name: desafio_mjv_api
    ports:
      - "8080:8080"
    volumes:
      - .:/app
    environment:
      - PYTHONUNBUFFERED=1
    command: uvicorn api:app --host 0.0.0.0 --port 8080 --reload
